You are modifying an existing Vite + React + TypeScript trading bot project.

MAKE THE FOLLOWING CHANGES PRECISELY AND DO NOT SIMPLIFY LOGIC.

1) CHANGE SOUND EFFECTS
- Replace the current WINNING sound (bell) with a short, clear "ding" sound.
- Replace the current LOSING sound with a low-frequency "beep" sound.
- Sounds must:
  - Play instantly with NO delay
  - Not overlap or stack
  - Reuse Audio objects instead of recreating them every tick
  - Be triggered ONLY by confirmed win/loss events

2) TRUE "RUN ON EVERY TICK" LOGIC (CRITICAL)
- The bot MUST process logic on EVERY tick generated by the WebSocket.
- Do NOT rely on React state updates to trigger analysis.
- Trading logic MUST run directly inside the WebSocket `onmessage` handler.
- No batching, throttling, debouncing, intervals, or delayed effects.
- The bot must NEVER skip or miss ticks.
- State updates should be UI-only and MUST NOT control core logic execution.

Correct pattern example:
- Process tick immediately when received
- Then update UI state

Incorrect pattern (DO NOT USE):
- setState → useEffect → analyze
- setInterval
- derived state-based triggers

3) REMOVE BOT & PROGRESS BAR LAG
- The progress bar must update in real-time with tick arrival.
- Progress updates should be driven by tick count or direct tick events.
- Avoid React re-render bottlenecks:
  - Use refs (`useRef`) for high-frequency values
  - Minimize state updates per tick
- UI should NEVER lag behind incoming ticks.

4) PERFORMANCE REQUIREMENTS
- No unnecessary re-renders per tick
- Use requestAnimationFrame if needed for smooth progress animation
- Ensure WebSocket connection remains stable and uninterrupted

5) DO NOT:
- Rewrite the entire app
- Remove existing features
- Add fake delays
- Add polling logic

ONLY modify the necessary files to achieve:
- Ding sound on win
- Low beep sound on loss
- 100% tick-accurate execution
- Zero progress bar lag

Explain briefly what files were changed and why.
